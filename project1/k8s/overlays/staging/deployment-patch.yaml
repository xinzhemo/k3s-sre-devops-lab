apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: demo
        env:
        - name: ENV
          value: "staging"
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
